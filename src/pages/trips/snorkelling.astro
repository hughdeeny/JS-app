---
import TripPageLayout from '../../components/TripPageLayout.astro';
import { tripPages } from '../../data/site';

const trip = tripPages.find((t) => t.slug === 'snorkelling')!;
---

<TripPageLayout trip={trip} />
